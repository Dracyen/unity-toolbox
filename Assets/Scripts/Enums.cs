public enum LockPickDifficulty
{
    EASY = 20,
    MEDIUM = 10,
    HARD = 5
}

public enum LayerDirection
{
    X = 0,
    Z = 1
}

public enum Direction
{
    NORTH,
    SOUTH,
    EAST,
    WEST
}

public enum Lane
{
    LEFT = 0,
    CENTER = 1,
    RIGHT = 2
}